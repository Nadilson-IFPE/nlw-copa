import { Center, Text } from 'native-base';

export function SingIn() {
    return (
        <Center flex={1} bgColor="grey.900">
            <Text color="white" fontSize={24} fontFamily="heading">SignIn</Text>
        </Center>
    );
}